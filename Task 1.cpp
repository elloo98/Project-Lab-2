#include <iostream>
#include <array>
#include <string>
using namespace std;
/*
bool is_sorted(int input[]) {
	bool sorted = true;
	int count = 0;
	int size = 9;
	
	while (count<size) {
		if (input[count] > input[count + 1]) {
			sorted = false;
		}
		count++;
	}
	return sorted;
}

int main() {
	int count = 0;
	int input[10];
	bool sorted;
	printf("Please enter ten natural numbers for the array:\n");
	while (count < 10) {
		cin >> input[count];
		count++;
	}
	sorted = is_sorted(input);
	printf(sorted ? "True" : "False");
	printf("\n");
	system("pause");
	return 0;
}
*/